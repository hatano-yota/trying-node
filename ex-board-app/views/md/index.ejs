<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body class="container">
    <header>
      <h1 class="display-4 text-primary"><%= title %></h1>
    </header>
    <main>
      <p class="h5 my-4">
        Hi, <span><%= login.name %></span>!<br />
        Welcome to <%= title %>.
      </p>
      <form method="post" action="/md">
        <div class="form-group">
          <label for="find">FIND</label>
          <input type="text" name="find" id="find" value="<%= form.find %>" class="form-control" />
        </div>
        <input type="submit" value="検索" class="btn btn-primary" />
      </form>
      <p class="my-4 h5"><%= message %></p>
      <table class="table">
        <% for (let i in content) { %>
        <tr>
          <td>
            <a href="/md/mark/<%= content[i].id %>" class="text-dark"> <%= content[i].title %></a>
          </td>
        </tr>
        <% } %>
      </table>
      <p class="mt-2"><a href="/md/add">※ データを登録</a></p>
    </main>
  </body>
</html>
